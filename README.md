# A 股 / 港股基本面排雷 Agent

输入股票名称或代码，系统识别公司，检索最新财报与公告，程序计算指标，
输出有原始证据、可下载离线阅读的 HTML 风险报告。

当前规则版本与报告版本为 **1.1**。2026-09-06 完成程序、计算、证据、任务调度和输出逻辑的专项修复；原有功能与 52 项规则保持不变，累计 **147 项自动测试通过**。旧版报告仍可查看，但须重新扫描才能应用 1.1 规则。

- 市场：沪市、深市、北交所、港股上市公司（基金/ETF/债券不进入扫描）
- 扫描：财务质量、偿债能力、公司治理、监管法律、经营行业五个维度
- 时间范围：最近 5 个完整财年 + 最新已披露财报 + 近 12 个月公告
- 行业适配：通用规则 + 银行、保险、券商、地产行业规则包
- 结论：每条绑定到具体披露文件与页码，数据不足的项明确标注，不输出"未发现异常"
- 不输出爆雷概率，不构成投资建议；报告提供**风险信号评分**（检查项扣分加权汇总，
  反映风险信号密度，不代表投资价值或违约概率）

## 文档索引

| 文档 | 内容 |
|---|---|
| `README.md`（本文） | 安装、启动、页面与 API、配置、常见问题 |
| `HANDOVER.md` | 交接报告：架构、设计决策、验收步骤与审核重点（供验收方使用） |
| `V1.2_DEVELOPMENT_HANDOVER.md` | 交给开发模型执行的 V1.2 可信度改造任务、边界与完成标准 |
| `docs/RULES.md` | 52 项检查规则完整参考：触发条件、阈值、适用行业 |
| `docs/PHASE0_DATA_VALIDATION.md` | 阶段 0 数据源验证报告（真实接口实测） |
| `DEVELOPMENT_STATUS.md` | 开发状态：已完成项、误报修复记录、未实现清单、续接指引 |
| `docs/ACCEPTANCE_DIAGNOSTIC_REPORT_2026-09-06.md` | 修复前的全面验收与 31 组问题诊断（历史快照） |
| `docs/FIX_REPORT_2026-09-06.md` | 31 组问题的逐项修复、回归证据和当前限制 |
| `docs/PERFORMANCE_OPTIMIZATION_2026-09-06.md` | 正式 Web 报告耗时基准、并行优化和前后质量对照 |
| `.env.example` | 全部配置项及注释（配置的唯一权威来源） |
| `A股港股基本面排雷Agent开发方案.md` | 原始开发方案（设计基线） |

## 快速开始

```bash
# 1. 安装依赖（Python 3.12+）
pip install -r requirements.txt

# 2.（可选）配置模型：复制 .env.example 为 .env，填入 LLM_API_KEY
#    不配置也能完整运行，AI 解读步骤会自动跳过并在报告中标注

# 3. 启动
python -m uvicorn app.main:app --host 127.0.0.1 --port 8770

# 4. 打开 http://127.0.0.1:8770
```

停止服务：如果服务在当前终端前台运行，按 `Ctrl+C`。如果它在后台运行，可按监听端口找到进程并正常停止：

```bash
PID=$(lsof -tiTCP:8770 -sTCP:LISTEN)
if [ -n "$PID" ]; then kill "$PID"; fi
```

命令行方式（不启动 Web）：

```bash
python scripts/run_scan.py 600519        # A 股：贵州茅台
python scripts/run_scan.py 00700         # 港股：腾讯控股
python scripts/run_scan.py 000002.SZ     # 显式证券代码，避免歧义
```

测试（共 147 项，全部离线，不需要网络与密钥）：

```bash
python tests/run_tests.py                                      # 原有测试：58 项
python -m unittest discover -s tests -p 'test_*.py' -v         # 验收回归与边界测试：89 项
```

阶段 0 数据验证（真实接口探测，约 2 分钟）：

```bash
python scripts/phase0_validate.py          # 默认 600519 + 00700
```

## Web 页面与 API

### 页面

| 路径 | 用途 |
|---|---|
| `/` | 首页：搜索证券（候选列表 + 歧义确认）、提交扫描 |
| `/scan/{task_id}` | 扫描进度页（轮询 8 个阶段直至完成） |
| `/report/{task_id}` | 在线报告（与下载内容完全一致） |
| `/download/{task_id}` | 下载独立 HTML 报告（离线自包含） |
| `/history` | 历史任务列表 |
| `/admin` | 运行状态：数据源健康、模型消耗、任务统计 |

### API

| 方法与路径 | 参数 | 说明 |
|---|---|---|
| `GET /api/suggest?q=` | `q`：名称或代码片段 | 证券搜索建议，返回候选列表 |
| `POST /api/scan` | body JSON：`{"query": "600519", "force": false}` | 提交扫描。同一查询 60 分钟内复用已有结果；`force=true` 强制重扫 |
| `GET /api/tasks/{task_id}` | — | 任务状态：阶段、进度、事实/公告数量、是否有报告 |
| `GET /api/health` | — | 健康检查：`llm_ready` 标识模型是否已配置 |

## 配置

所有配置通过环境变量或 `.env` 文件读取，全部有安全默认值，零配置可运行。
完整清单见 `.env.example`，常用项摘要：

| 配置 | 默认 | 说明 |
|---|---|---|
| `LLM_API_KEY` | 空 | 模型密钥。不配置则 AI 解读/核验整体跳过并标注 |
| `LLM_BASE_URL` / `LLM_MODEL` | DeepSeek | 任意 OpenAI 兼容服务商均可 |
| `LLM_BUDGET_CNY` | 3.0 | 单次扫描模型预算上限（元），超出自动降级 |
| `LLM_PARALLEL_CALLS` | 2 | 独立模型批次并发数；不改变输入与校验，供应商限制并发时可设为 1 |
| `FISCAL_YEARS_BACK` | 5 | 回看的完整财年数 |
| `ANNOUNCEMENT_MONTHS` | 12 | 公告检索区间（月） |
| `MAX_PDF_DOWNLOADS` | 25 | 单次扫描原文下载份数上限 |
| `SCAN_TIMEOUT_SECONDS` | 900 | 任务超时（秒），超时生成带缺口的部分报告 |
| `PER_HOST_RPS` | 2.0 | 每主机请求频率上限，对数据源保持克制 |
| `ALLOW_PRIVATE_ADDRESS` | false | 保持 false，防 SSRF |

## 数据来源

| 数据 | 来源 | 鉴权 | 说明 |
|---|---|---|---|
| A 股/港股财报与主要指标 | 东方财富数据中心公开接口 | 无 | 三张报表 + 主要指标，含公告日期、报告期类型 |
| A 股公告与原文 | 巨潮资讯网 | 无 | 深交所法定披露平台 |
| 港股公告与原文 | 港交所披露易 | 无 | 公开网页接口（非 IIS 授权信息流） |
| 公司资料 | 东方财富 F10 | 无 | 全称、行业、审计机构、实际控制人、曾用名 |
| AI 解读/核验 | 任意 OpenAI 兼容服务 | 需 API Key | 不配置则自动跳过，不伪造结论 |

**权限注意**：以上均为公开网页接口，无账号鉴权。适合本地开发与内测；
公开部署或商业使用前，需与各数据方确认接入条款与再分发授权（见 DEVELOPMENT_STATUS.md 已知限制）。

## 工作流程（方案 §4 固定主流程）

```
公司识别 → 检索规划 → 资料获取 → 标准化 → 规则检查 → 专项阅读 → 核验 → 报告生成
```

- **程序负责**：获取与追踪来源、计算指标、执行规则、校验证据引用、管理任务状态、渲染安全 HTML
- **AI 负责**（可插拔）：理解附注与公告、解释已算出的异常、识别缓解因素、独立核验结论与证据的一致性
- 模型不参与任何数值计算；未配置密钥时相关步骤整体跳过并标注，绝无模拟结论
- 相互独立的模型批次默认最多 2 路并行；输入、模型参数、预算、校验和合并顺序保持不变

每条风险按统一结构呈现：

> 发现了什么 → 为什么值得关注 → 证据是什么（原文片段 + 页码 + 指纹）→
> 当前是否仍存在 → 有哪些缓解因素 → 还需核实什么

## 规则体系

共 52 项配置化规则（完整清单、阈值与适用行业见 `docs/RULES.md`）：

- 通用规则 36 项（财务质量 12 / 偿债 8 / 治理 6 / 监管 5 / 经营 5）
- 行业包：银行 4 项、保险 4 项、券商 4 项、地产 4 项
- 每个检查项只输出五种结论之一：发现风险 / 需要关注 / 已覆盖资料中未发现明显异常 / 数据不足，无法判断 / 不适用
- 银行、保险、券商自动跳过不适用的普通企业财务结构规则（资产负债率、流动比率等），
  地产的杠杆判断由"剔除预收后的资产负债率"（RE01）替代普通资产负债率（SV02）
- 严重程度与证据强度分别表达；重要性过滤：应收/存货占总资产 < 2% 时增速类指标不形成结论

## 报告

- 标题格式：`证券简称（代码）排雷报告`，如"兆易创新（603986.SH）排雷报告"
- 在线阅读：`/report/{task_id}`（与下载内容完全一致）
- 独立 HTML 下载：`/download/{task_id}`——内联 CSS + 内联 SVG 图表，
  无外部脚本/字体/图片依赖，离线可读、可打印；手机端将五列表格转换为带字段标签的卡片，避免缩列和横向滚动
- 响应式布局：PC 保留完整表格；640px 以下优化标题、目录、评分、触控折叠区和卡片间距，360px 以下切换为单列目录与元数据
- 动态文字统一转义，来源链接仅允许 http/https
- 章节结构（4 节，附可点击跳转的报告目录）：
  1. **风险总览**——风险信号评分、结论速览与发现清单
  2. **逐项检查结果**——五个维度逐条结论（"不适用"与"数据不足"行不显示；长说明折叠）
  3. **问题详情与原文**——每条结论的完整证据链（原始证据默认折叠，打印时自动展开）
  4. **数据来源与算法**——AI 参与情况、计算口径与免责声明

### 风险信号评分

- 计算方式：基础分 100，按检查项扣分——发现风险 高-12 / 中-8 / 低-4，
  需要关注 中-2 / 低-1，下限 0；"数据不足"与"不适用"不扣分
- 评级分档：A ≥ 95 / B ≥ 80 / C ≥ 60 / D ≥ 35 / E < 35
- 计算位置：`app/report/render.py` 的 `risk_signal_score()`（程序计算，模板只展示）
- 免责口径：评分为检查项扣分加权汇总，反映风险信号密度，不代表投资价值或违约概率

## 目录结构

```
app/
├── main.py              # FastAPI 入口：页面、API、后台任务
├── config.py            # 配置（环境变量 / .env）
├── core/                # 数据对象、SQLite 存储层、安全 HTTP 客户端、文本工具
├── data/                # 数据适配层：东财财报、巨潮公告、披露易公告、PDF 解析、身份识别
├── engine/              # 标准化、指标计算、规则引擎（通用 + 行业包）、流水线编排
├── llm/                 # 可替换模型适配层（OpenAI 兼容协议）
├── report/              # 报告渲染（受控 Jinja2 模板 + 内联 SVG 图表）
└── web/                 # 页面模板与静态资源
scripts/                 # run_scan（CLI 扫描）、phase0_validate（数据验证）、inspect_report（报告摘要检查）
tests/run_tests.py       # 58 项原有确定性测试（离线）
tests/test_acceptance.py # 53 项验收回归测试
tests/test_hardening.py  # 36 项独立边界与集成测试
docs/                    # 规则参考、阶段 0 验证报告
data/                    # SQLite、下载原文、生成的报告（运行时产生，勿提交）
data/publish/            # 外部分享链接的静态发布目录（单报告一目录，勿提交）
```

## 修复后端到端复验样本

| 样本 | 市场 | 行业包 | 结果摘要 |
|---|---|---|---|
| 贵州茅台 600519.SH | A 股 | general | 规则 1.1 真实扫描完成；公告、PDF、证据、财务指标和模型失败显式化链路通过 |
| 腾讯控股 00700.HK | 港股 | general | 正式 Web 扫描生成 52 项结果；财报币种核实为 CNY，可用现金口径修正；在线与下载逐字节一致 |
| 万科A 000002.SZ | A 股 | realestate | 负利润样本复验：4 项风险、8 项关注、8 项数据不足；亏损、持续经营审计信号及缺失地产科目按新口径处理 |

以上是特定时点和已获取资料范围内的程序复验，不是长期固定的投资结论。数据源更新、PDF 页数上限或模型供应商截断都可能使扫描显示“部分完成”，具体缺口会写入报告。旧规则版本的其他样本分数不再作为当前版本验收结论。

## 常见问题

**Q：扫描报网络错误 / 公告获取为 0 条？**
依次检查：本机是否开了系统代理（代理会拦截对数据源的请求，可在终端 `unset http_proxy https_proxy` 后重启）；
数据源接口是否临时变更（查看 `/admin` 页数据源健康状态；
运行 `python scripts/phase0_validate.py` 定位断在哪个环节）。

**Q：提示"已有 60 分钟内的扫描结果"？**
同查询 60 分钟内去重复用。需要重新扫描时在提交页勾选"强制刷新"，或 API 传 `"force": true`。

**Q：报告里大量"数据不足，无法判断"？**
正常设计，不是缺陷。行业监管指标（资本充足率、偿付能力充足率等）当前数据源未提供结构化字段，
按方案要求明确标注而非估算。此类检查项在"逐项检查结果"中不显示（不形成结论），
总数和逐项原因在"风险总览"的折叠清单中汇总。

**Q：如何验证报告里的证据是真的？**
每条证据带文件名、页码和内容指纹，可在"数据来源与算法"一节回溯到原始公告 URL；
程序在生成后还会做一次原文复核（下载文件重新解析比对），复核未通过的会在报告中标注。

**Q：想调整规则的阈值？**
规则定义在 `app/engine/rules/general.py`（通用）和 `app/engine/rules/industry.py`（行业包），
阈值写在各 `_r_xx` / `_bk01` 等检查函数内，改完运行 `python tests/run_tests.py` 回归。

## 已知限制

完整清单见 `DEVELOPMENT_STATUS.md` 第四节，摘要：

- AI 解读与核验已启用（GLM-5.3-Flash，OpenAI 兼容协议可随时换服务商）；
  未配置密钥时相关步骤整体跳过并标注，不影响扫描
- 20 家固定验证集回归未建立（当前有 3 个规则 1.1 真实复验样本，另有旧版历史样本）
- 历史风险跨年追溯未实现（当前仅 12 个月公告窗口）
- 数据源商用授权未确认，公开部署前需与数据方核实
