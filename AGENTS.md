# Global Working Agreements

## 数据提取与计算的强制链路

凡任务涉及数据提取、清洗、转换、计算、分析、核对或报表生成，必须遵循以下链路：

**原始数据源 → Raw 逐笔数据 → 计算和审核底稿 → 最终结果**

### 1. 原始数据源

- 记录数据来源、提取时间、数据期间、查询或筛选参数及可识别的版本信息。
- 不修改原始来源，不直接在原始文件或原始响应上进行计算。
- 如原始来源是数据库、API 或网页，保留可复现的查询、请求参数或提取脚本，以及必要的来源说明。

### 2. Raw 逐笔数据

- 保存从原始数据源提取的、未经汇总的逐笔无损数据。
- 尽量保留原始字段名、原始值、稳定主键以及来源追踪字段。
- Raw 层不得包含业务计算、人工调整、映射结果或对原始值的覆盖。
- 后续发现提取逻辑需要变更时，重新生成带版本的新 Raw 数据，不静默改写既有 Raw 数据。

### 3. 计算和审核底稿

- 所有清洗、类型转换、映射、关联、筛选、公式、假设、排除项及人工调整必须显式记录。
- 计算过程必须可重复执行，优先使用脚本、查询或保留公式的工作簿，不以无法追踪的手工操作代替。
- 明确记录输入、处理步骤、输出及其对应关系，使每项最终结果能够追溯到 Raw 逐笔数据。
- 执行并记录必要的审核检查，包括但不限于记录数、金额或数量合计、缺失值、重复值、主键唯一性、关联未匹配项、期间范围及异常值检查。
- 对原始数据、Raw 数据、底稿结果和最终结果进行必要的勾稽；任何差异、排除项或人工调整都必须注明原因和影响。

### 4. 最终结果

- 最终结果只能从已经完成审核的底稿生成，不得直接从 Raw 数据或原始来源手工拼接。
- 标明数据期间、单位、口径、生成时间及版本。
- 保留指向所使用 Raw 数据、计算逻辑和审核底稿的明确路径或引用。
- 最终结果不得包含未记录在底稿中的手工修改。

### 5. 目录与交付约定

在项目没有既定结构时，优先使用：

```text
data/source_manifest/   # 来源、期间、查询参数和版本记录
data/raw/               # Raw 逐笔数据
workpapers/             # 计算脚本、映射表、审核与勾稽底稿
outputs/                # 最终结果
```

- 不得无说明地跳过任何一层。若受权限、技术限制或数据敏感性影响无法完整落地，必须说明缺失层、原因、风险及替代控制措施。
- 交付时列出各层产物的位置，并简要说明已执行的审核检查、发现的差异或例外以及最终结论。
- 遵守适用的数据安全、隐私和保留要求；不得把凭据、访问令牌或不必要的敏感数据写入产物。
