# A 股 / 港股基本面排雷 Agent 开发方案

版本：V1.0 · 待审核

## 1. 产品目标与交付边界

用户输入股票名称或代码，系统识别公司，检索最新可获取的财报和公告，完成风险检查，输出有原始证据、可下载的 HTML 报告。

产品应回答三个问题：

- 这家公司目前有哪些值得关注的基本面风险？
- 判断依据是什么，发生于何时，目前是否仍然存在？
- 哪些事项因为资料不足，还不能下结论？

“实时”定义为用户发起扫描时刷新可获取的公开资料。页面分别展示扫描时间、公告检索时间和财务报告期，不使用“实时财务数据”等容易误解的表述。

**第一版完整交付范围：**

| 项目 | 交付内容 |
|---|---|
| 市场 | 沪市、深市、北交所、港股上市公司 |
| 查询 | 公司名称、简称、证券代码、曾用名匹配 |
| 扫描 | 财务、偿债、治理、监管法律、经营五个维度 |
| 时间范围 | 最近 5 个完整财年、最新已披露财报、近 12 个月公告 |
| 历史风险 | 对发现的未结事项向前追溯，并检索后续进展 |
| 行业适配 | 普通企业规则，以及银行、保险、券商、地产专用检查项 |
| 报告 | 在线阅读、证据展开、独立 HTML 下载、打印 |
| 任务管理 | 扫描进度、失败重试、历史报告、重新扫描 |
| 运维 | 数据源状态、任务耗时、调用成本、失败记录 |

基金、ETF、债券、权证等非公司证券不进入扫描流程。暂不做股价预测、交易、投资组合、自动买卖和订阅提醒。

新增上市公司历史不足 5 年时，按实际披露范围检查，不据此认定异常。

## 2. 页面与用户体验

### 首页

以股票搜索框为中心，附示例输入和最近扫描记录。

搜索结果展示：

- 公司全称和简称；
- 股票代码和交易所；
- 上市地及交易币种；
- 所属行业。

同名、模糊匹配及 A/H 双重上市必须展示候选项，由用户确认证券。系统在后台关联共同的公司主体。

### 扫描页

展示真实阶段：

公司识别 → 收集资料 → 提取和核对 → 风险检查 → 生成报告。

显示已获取资料数、当前阶段及数据缺失提示。用户离开页面不会中止后台任务，返回后可以恢复查看。

不展示虚构百分比；有明确总量的步骤才显示完成比例。

### 报告页

顶部先呈现：

- 公司身份和数据时间；
- 已识别的最高风险等级；
- 最重要的 3—5 项发现；
- 检查覆盖情况；
- 关键资料缺失。

下方提供风险详情、财务趋势、事件时间线和来源清单。每条结论可展开查看证据片段及原文定位。

若存在高风险但资料不完整，同时展示“已发现高风险”和“评估覆盖不足”，不能让其中一项掩盖另一项。

### 历史记录与管理页

用户查看自己的任务和报告版本。管理员查看数据源异常、失败任务和消耗，不在普通用户页面暴露接口等技术细节。

第一阶段采用邀请制内测和基础登录，不实现收费系统；为后续公开注册保留扩展位置。

## 3. 数据接入方案

采用两条数据链路：

- **结构化数据**：用于计算财务指标、识别趋势；
- **原始披露文件**：用于解释异常、核验重要事实和提供证据。

不能只依赖搜索引擎，也不能把数据服务商返回的指标直接当作完整风险报告。

| 数据类型 | 首选接入方向 | 降级处理 |
|---|---|---|
| 证券与公司主数据 | 有授权的数据接口，结合交易所资料核对 | 无法可靠确认身份时停止扫描 |
| A 股财务数据 | Tushare 等结构化服务，正式财报交叉核验 | 从财报提取，标注提取质量 |
| A 股公告 | 巨潮、交易所披露及获授权的数据服务 | 公司投资者关系网站补充 |
| 港股财务数据 | 有港股权限的结构化服务，结合正式财报 | 按原文科目提取并保留会计口径 |
| 港股公告 | 港交所披露易及获授权的公告服务 | 公司投资者关系网站补充 |
| 监管与法律事项 | 监管公告、交易所文件、公司披露 | 新闻仅作为待核实线索 |
| 行业事件 | 正式行业资料和可信新闻 | 无法验证时不形成确定结论 |

已核实的接入情况：

- 巨潮是深交所法定信息披露平台，并有单独的数据服务平台；具体接口权限和可用范围需要在接入阶段验证。[巨潮资讯](https://www.cninfo.com.cn/new/index.jsp)、[数据服务平台](https://webapi.cninfo.com.cn/)
- Tushare 提供港股利润表接口，官方文档注明需要单独开通权限。因此不能假定 A 股权限自动覆盖港股。[港股利润表文档](https://tushare.pro/document/2?doc_id=389)
- 港交所提供 IIS 公告信息流服务；是否适合本产品，需要确认接入条件及使用授权。[港交所 IIS](https://www.hkex.com.hk/Services/Market-Data-Services/Infrastructure/Issuer-Information-feed-Service-%28IIS%29?sc_lang=en)

以上是文档核实，尚未进行账号鉴权、真实接口测试或采购。

**数据接入阶段必须确认四件事：**

1. 能否稳定获取目标公司的完整资料；
2. 更新延迟、历史覆盖、请求频率是多少；
3. 是否允许商业使用、缓存、模型处理和报告展示；
4. 是否允许在下载报告中引用片段或展示衍生指标。

公开可阅读不等于可以不限量抓取或再分发。生产环境优先使用稳定、可授权的接入方式。

如果港股数据权限尚未落实，可以继续完成产品工程，但不能把港股功能标记为已验收。

## 4. Agent 执行流程

采用固定主流程，并允许在缺少关键证据时进行有限补充检索。

| 步骤 | 系统执行内容 | 输出 |
|---|---|---|
| 1. 身份识别 | 匹配证券、公司、行业、A/H 关系 | 唯一公司与证券标识 |
| 2. 检索规划 | 根据市场、行业、报告期生成资料清单 | 必需资料与补充资料 |
| 3. 资料获取 | 分页收集公告、下载财报、去重 | 文件索引与获取记录 |
| 4. 标准化 | 提取表格、币种、单位、科目和时间 | 财务事实表 |
| 5. 规则检查 | 程序计算指标，判断规则是否触发 | 候选异常 |
| 6. 专项阅读 | AI 阅读相关附注、公告和事件进展 | 有证据的风险解释 |
| 7. 核验 | 检查主体、日期、来源和结论是否对应 | 核验后的风险项 |
| 8. 报告生成 | 结构化结果通过受控模板渲染 | HTML 和结构化报告 |

**AI 的职责：**

- 理解财报附注和公告；
- 提取事件及相关主体；
- 解释已计算出的异常；
- 识别缓解因素和后续进展；
- 用清晰中文组织报告。

**程序的职责：**

- 获取、储存和追踪来源；
- 计算指标；
- 执行规则和行业适配；
- 检查证据引用是否真实存在；
- 管理任务状态、缓存和消耗；
- 生成安全、稳定的 HTML。

第一版采用单一工作流中的不同模型步骤，不引入多 Agent 自由讨论。对高风险事项增加独立核验步骤，但模型之间的相互认可不作为事实成立的依据。

公告检索记录分页完成情况；文件因数量上限、下载失败或时间限制未被检查时，报告必须列出缺口。

## 5. 财务口径与证据设计

每条财务数据保存：

- 原始科目及标准科目；
- 数值、单位和币种；
- 报告期、公告日期和获取时间；
- 合并或母公司口径；
- 年度、半年度、累计或单季口径；
- 是否经审计、是否重述；
- 文件来源、页码或表格位置；
- 提取方式及核验状态。

处理原则：

- 原始值保留，标准化结果另外储存；
- 同期比较必须使用一致口径；
- 利润为负、接近零时，不套用普通同比解释；
- 不把半年度数据直接当作全年数据；
- 不把受限资金直接计为可自由偿债现金；
- 银行等特殊行业不套用普通企业负债率标准；
- 港股不同会计准则和财年差异保留标记；
- A/H 公司财务事实按主体去重，市场特有事项分别保留。

发生财务重述时，新报告采用适用的重述口径并说明变化，旧报告保持原始扫描版本，不能悄悄覆盖。

证据保存发布机构、标题、URL、发布时间、原文片段、页码及内容指纹。储存原始文件的范围和期限遵循数据授权。

## 6. 风险规则体系

规则以配置方式维护，每条包含适用行业、所需数据、计算方式、触发条件、严重程度、解释模板和版本。

首版目标为约 30—40 个检查项，最终数量以数据覆盖和验证质量为准。

| 维度 | 重点检查 |
|---|---|
| 财务质量 | 利润与经营现金流背离、应收和存货异常、非经常性收益依赖、减值、资本化和财务重述 |
| 偿债能力 | 短债与可用现金、利息负担、债务到期、违约、担保和持续经营问题 |
| 公司治理 | 股权质押冻结、资金占用、关联交易、控制权变化、审计机构与高管异常变更 |
| 监管法律 | 调查处罚、重大诉讼、资产冻结、披露违规及上市地位风险 |
| 经营行业 | 客户供应商集中、主要业务恶化、资质变化、并购和大额资本开支 |

**特殊行业分别建规则包：**

- 银行：资产质量、拨备、资本充足及集中度；
- 保险：偿付能力、承保表现、投资资产和准备金；
- 券商：净资本、流动性、信用及自营风险；
- 地产：债务履约、受限现金、交付义务和项目减值。

具体监管阈值在实施时根据适用市场和规则版本核实，不由模型自行生成。

每个检查项只能输出：

1. 发现风险；
2. 需要关注；
3. 在已覆盖资料中未发现明显异常；
4. 数据不足，无法判断；
5. 不适用。

严重程度与证据强度分别表达。例如“潜在影响较大、证据尚不足”不能改写成“已确认重大风险”。

不输出爆雷概率或未经校准的安全分数。

覆盖情况展示“已完成检查数 / 适用检查数”，另外列出关键缺失资料。即使普通检查项大部分完成，只要缺少最新财报或重要审计文件，也不能给出充分评估的印象。

## 7. 技术架构

建议使用以下组合，具体依赖版本在实施时锁定：

| 层次 | 方案 | 用途 |
|---|---|---|
| 页面 | Next.js + TypeScript | 搜索、进度、报告和历史记录 |
| 后端 | Python + FastAPI | 数据适配、指标计算、模型工作流 |
| 任务执行 | Celery + Redis | 后台扫描、限流和重试 |
| 数据库 | PostgreSQL | 公司、财务事实、证据和报告版本 |
| 文件存储 | S3 兼容对象存储 | 授权范围内的文件、解析结果和 HTML |
| 模型接入 | 可替换的统一适配层 | 提取、分析和核验 |
| 报告渲染 | 固定模板 + 内联 CSS / SVG | 离线阅读、打印及安全展示 |
| 部署 | Docker Compose 起步 | 开发及小规模内测 |

前端负责交互，后端持有业务逻辑和密钥。模型供应商可以替换，不把规则和数据结构绑定到某一家模型。

首版不引入独立向量数据库；先按公司、报告期、公告类型和章节定位资料，需要语义检索时再扩展。

主要数据对象包括：公司、证券、披露文件、财务事实、证据、风险事件、规则结果、扫描任务和报告版本。

任务状态包括：排队、运行、部分完成、完成、失败、取消。每个阶段保存检查点，进程重启后可以继续或安全重试。

对重复提交执行去重；同一公司、同一数据快照、同一规则版本可以复用计算结果，但新扫描必须先检查最新资料索引。

## 8. HTML 报告结构

报告依次包含：

1. 公司身份、扫描时间和资料范围；
2. 执行摘要；
3. 风险总览及覆盖情况；
4. 五个维度的检查矩阵；
5. 核心财务趋势与计算口径；
6. 风险详情及原始证据；
7. 事件时间线；
8. 缓解因素和已解除事项；
9. 缺失资料与下一步核实清单；
10. 来源、方法和报告版本。

每条风险采用统一结构：

> 发现了什么 → 为什么值得关注 → 证据是什么 → 当前是否仍存在 → 有哪些缓解因素 → 还需核实什么。

离线报告内嵌样式和 SVG 图表，不依赖外部字体或脚本。原始来源链接需要联网访问，报告正文、图表和必要证据片段可离线阅读。

动态文字统一转义，来源链接只允许安全协议。报告使用受控模板，模型不直接生成可执行页面代码。

## 9. 可靠性、安全与成本控制

### 可靠性

- 对可恢复错误进行有限重试；
- 按数据源设置限流和熔断；
- 关键来源失效时明确降级；
- 限制单次任务文件数、解析页数、检索轮数和模型消耗；
- 达到预算上限时生成带缺口的部分报告，不静默省略。

### 安全

- 密钥只在服务端保存；
- 报告和任务绑定用户权限；
- 限制文件大小、解析时间和下载地址；
- 防止下载器访问内网地址；
- 外部文档中的指令按数据处理，不能改变 Agent 行为；
- 不向用户暴露模型内部推理，仅提供证据和可解释的结论。

### 运行目标

以下是待实测的工程目标，不是现阶段承诺：

| 场景 | 初始目标 |
|---|---|
| 股票搜索 | 常规请求 2 秒内 |
| 已有资料的刷新扫描 | 1—3 分钟 |
| 首次完整扫描 | 3—10 分钟 |
| 超时处理 | 约 15 分钟结束为部分报告或明确失败 |
| 内测规模 | 先验证 5 个并行扫描任务 |

复杂扫描件、数据源延迟和特殊行业可能超出目标，阶段验收后根据真实结果调整。

单次成本按以下项目计量：

数据接口费用 + 文件解析/OCR + 模型输入输出 + 存储及服务分摊。

通过文件去重、增量解析、相关章节检索和规则先行控制费用。先跑真实样本再确定人民币预算，不在模型和数据授权未选定时给出虚构单价。

## 10. 开发阶段与交付物

以下按一名全栈工程师配合 AI 开发估算，约 24—33 个工作日；不包含外部数据授权等待时间。

| 阶段 | 预计时间 | 交付物与通过条件 |
|---|---:|---|
| 0. 数据验证 | 2—3 天 | A/H 样本真实接入、权限矩阵、覆盖和费用说明；两市场均验证财报与公告 |
| 1. 产品骨架 | 3—4 天 | 登录、证券查询、后台任务、真实进度、历史记录 |
| 2. 资料与证据 | 5—7 天 | 公告采集、PDF 解析、财务标准化、原文定位和版本保存 |
| 3. 风险引擎 | 6—8 天 | 通用与行业规则、AI 解读、重大风险核验、结构化报告 |
| 4. 报告交付 | 3—4 天 | 在线报告、趋势图、下载、打印及移动端适配 |
| 5. 验收与内测 | 5—7 天 | 样本回归、故障测试、权限检查、部署和运维文档 |

阶段 0 是首个决策点：如果无法获得可靠且适用的数据接入方式，应调整供应商或覆盖承诺，再投入完整分析引擎开发。

阶段 3 可先贯通普通行业作为内部里程碑；特殊行业完成前，不算第一版完整验收。

## 11. 验收方法

建立至少 20 家公司的固定验证集，覆盖：

- A 股、港股及 A/H 双重上市；
- 普通行业、银行、保险、券商和地产；
- 已披露明确风险的案例；
- 历史风险已解除的案例；
- 新上市、资料不足及财务重述案例。

另外建立确定性测试样本，覆盖负利润、零分母、币种与单位转换、累计值和单季值等边界。

验收要求：

| 项目 | 通过标准 |
|---|---|
| 公司识别 | 固定测试集中身份和市场匹配正确，歧义必须提示 |
| 财务计算 | 预设计算样例全部通过 |
| 引用真实性 | 不允许出现虚构来源或无法关联的证据 ID |
| 高风险结论 | 全部有原始披露证据，且人工抽核主体、时间和语义 |
| 已知风险 | 固定案例中预先标注的关键风险不得漏检 |
| 已解除事项 | 不得错误描述为当前仍存在 |
| 资料不足 | 必须标注缺失，不能输出确定的无风险判断 |
| 行业适配 | 特殊行业不触发不适用的普通企业规则 |
| 任务恢复 | 超时、限流、重启和重复提交均有确定结果 |
| 报告 | 手机、电脑、离线和打印均可用 |
| 安全 | 权限隔离、恶意链接和文档指令注入测试通过 |

这些验收结果仅说明测试范围内的表现，不转述为全市场准确率。后续积累人工标注案例，再评估风险召回率和误报率。

## 12. 主要风险与应对

| 风险 | 对产品的影响 | 应对 |
|---|---|---|
| 数据权限或覆盖不足 | 无法稳定支持某市场 | 阶段 0 验证，供应商可替换 |
| 公告遗漏 | “未发现”结论不可靠 | 记录索引范围、分页和失败文件 |
| PDF 表格提取错误 | 财务指标及结论出错 | 口径核对、关键表勾稽、重大异常回原文 |
| 模型过度推断 | 将线索描述成事实 | 结构化输出、证据绑定和独立核验 |
| 行业阈值不适用 | 大量误报 | 行业规则包与不适用状态 |
| 长文档成本过高 | 扫描慢、费用失控 | 增量解析、章节定位和预算限制 |
| 数据源变化 | 扫描失败率上升 | 适配层隔离、健康监控和回归样本 |

## 13. 本次建议审核通过的默认决策

- 产品形态：独立 Web 应用，先邀请制内测。
- 首版市场：A 股和港股均纳入，分阶段实现。
- 数据策略：允许接入付费服务；具体供应商及采购金额在阶段 0 后单独确定。
- Agent 形式：固定工作流、程序计算、AI 解释、证据核验。
- 输出：在线报告和独立 HTML，不输出安全分数或爆雷概率。
- 技术方案：Next.js + Python 后端 + PostgreSQL + 后台任务队列。
- 首个实施里程碑：真实跑通一家 A 股和一家港股公司的资料获取、指标计算、证据定位及报告生成。
- 完整交付标准：包括行业适配、故障处理、报告下载和固定案例验收，不以页面演示作为完成。

方案通过后，先执行阶段 0，交付真实样本报告、数据覆盖说明和费用测算，再据此校准后续排期。
