# 项目程序修复与回归记录

日期：2026-09-06。对应诊断报告 A01—A31。修复对象为程序、计算与判断、资料和证据处理、任务调度及输出逻辑；历史报告只用于兼容性与复现检查，没有逐份篡改结论。

保留原有 52 项检查、五个行业包、四节报告结构及评分功能。规则版本升级为 **1.1**；旧版扫描不再被新版缓存复用，查看旧报告会提示重新扫描。

## 验证结果

- 原有测试 **58/58 通过**。
- 从验收用例迁移的回归 **53/53 通过**。
- 新增独立边界与集成检查 **36/36 通过**。
- 合计 **147 项通过**，没有通过删除失败规则或隐藏错误来实现通过。
- 当前工作区仅含交付源码的独立目录可导入应用，并注册完整 **52 项规则**。`app/data` 六个文件及新增修复模块均已纳入版本控制。
- 浏览器实际验证：更改输入后提交新证券；候选脚本按文字展示、不执行；点击索引展开原文；390/640 像素下表格保留可读列宽，局部可以横向滚动。
- 修复前的 **18 份历史 JSON** 加上本轮正式 Web 生成的 1 份新版 JSON，共 **19/19** 在严格模板下可渲染。历史文件只是兼容性检查，不是本轮修复对象，也不代表财务正确性的标注样本集。
- A/H 两市场真实扫描、万科负利润样本及正式 Web 扫描均已完成。明细分别见 `fixes-2026-09-06/live_scan_v11.log`、`fixes-2026-09-06/vanke_final_validation.log` 和 `fixes-2026-09-06/web_scan.json`。真实模型有时仍返回截断输出，程序会保留已有有效结果、标注缺口；不把外部调用成功等同于解读有效。

## 逐项处理

| 编号 | 程序修复 | 验证与边界 |
|---|---|---|
| A01 | `.gitignore` 的 `data/` 改为 `/data/`；适配源码纳入版本控制；保留用户原有 `.workbuddy/` 忽略项 | 仅导出交付源码，在没有原数据库、PDF、`.env` 的临时目录成功导入应用并注册 52 项规则 |
| A02 | 禁止自动盲跟重定向，逐跳校验；拒绝非公网、映射 IPv6、组播等地址；连接时重新校验 DNS 并连接验证后的 IP，TLS 使用原域名；DNS 等待有界 | 请求与下载重定向、直接内网、IPv6、组播、DNS 超时、连接 IP 固定测试；真实公网查询通过 |
| A03 | 候选项使用文本节点，移除外部字段的 `innerHTML` 拼接 | 真实浏览器中候选包含 `img onerror`，标签数量为 0、执行标记为空 |
| A04 | 输入变化清空旧选择；丢弃过期搜索响应；模糊匹配需确认；过滤基金/指数等类别；精确代码核实市场；兼容真实 `AStock` 返回值 | 真实输入修改提交 `00700`；模拟歧义、基金、错误交易所及真实接口形状测试 |
| A05 | FQ01 先区分利润符号；亏损时盈利覆盖倍数不适用，亏损和现金净流出由已有规则检查；零分母无法计算 | 正正、正负、负正、负负、零组合均验证，保留 FQ02/FQ08/OP05 |
| A06 | 修正应收重要性指标键为 `ar_to_assets` | 由指标计算进入 FQ03 的小基数测试通过 |
| A07 | 合法零值不再被 `or` 回退覆盖；可用现金、权益与费用的零值保留 | 全部现金受限时可用现金为 0，SV01 判风险；零权益不会被备用字段覆盖 |
| A08 | 受限现金、商誉分母、地产预收及合同负债缺失时明确数据不足；有息负债不再将未知长借/短借当 0 | 缺失/零值成对测试；适用规则输出数据不足而不是正常 |
| A09 | 分开证券币种与报表币种；港股通过原文单位及至少两项关键金额交叉核对；未核实期不套用交易币种；现金及等价物单独映射，不重复扣减受限现金 | 腾讯真实扫描及构造金额核对通过；未核实历史期间如实标注，原始金额完整保留 |
| A10 | RE01 分子分母均剔除预收款与合同负债；字段须齐备、调整后分母须有效；去掉自动断言违反当前监管要求的文案 | 显式合同负债为 0 时 `(80-20)/(100-20)=75%`；缺失合同负债则数据不足 |
| A11 | 修正 `pending_evidence` 属性访问 | 正式 OP02 → 证据绑定 → 原文核验 → RuleResult 链路测试通过 |
| A12 | 支持审计简繁字形；排除标准无保留；空正文/没有意见段不判正常；引用保留原字形 | 简繁肯定与否定、标准无保留、空正文及真实片段测试通过 |
| A13 | 事项分类优先于年报/业绩载体；审计机构优先于普通聘任；增加业绩预告分类；解除冻结/质押、不减持等与当前负面事项区分 | 更正/问询/审计机构/盈利警告/解除冻结等成对测试 |
| A14 | 核验完整规范化引文、文档身份、物理页码和指纹 | 错文档、错页、伪造尾部和篡改指纹均被拒绝 |
| A15 | 证据 ID 加入片段指纹；按独立 `doc_id` 查原文；解析对象绑定明确文档身份 | 同页多主题不覆盖，精确定位证据再次核验通过 |
| A16 | 增加科目与金额所在行的定向定位；取消任意同维度年报兜底；事件绑定实际命中文件；关键词或金额行相关不自动升级为“原始披露确认” | 通讯地址页不会成为数值证据；未完成单位/期间/全部依赖项核实的仍为部分支持或待核实。不会假装所有数值结论已被 PDF 独立复算 |
| A17 | 报告显示有效覆盖、真实缺口、PDF 截断、AI 失败与核验结果；缺失项放在折叠清单；零判断不显示 100A；保留评分算术 | 页面与模板测试；真实截断失败已显示；仍保留四节结构 |
| A18 | 模型事件来源限制为本批输入 ID，未知来源拒收并记录；日期采用已提供公告日期 | 不存在 ID、不在输入批中的来源不会落成有效事件 |
| A19 | 空页/解析失败不读取 `pages[0]`；模型错误结构按失败处理，保留其他有效结果 | 模型开关、空 PDF、错误结构、部分批次成功均有测试 |
| A20 | 阶段、HTTP 重试/限流/DNS、流式读取和 LLM 共享期限；PDF 在可终止子进程中解析，每份最多等待 60 秒或剩余任务时长 | 到期不进入后续财务请求；超时前不调用传输；PDF 超时终止；DNS 等待有界 |
| A21 | 同比指定上一年同期；检查币种、单位、期次和期间；保留所有原始事实版本、计算只选合并口径及最新公告；规则取同一期事实，避免旧商誉除以新资产 | 缺年、币种不一致、母公司覆盖、倒序重述、旧科目混用测试 |
| A22 | 普通口径说明与实际缺口分开；同进程在途提交原子复用；允许同版近期部分报告复用；队列有界；旧规则版缓存失效 | 并发提交、纯说明完成状态、部分缓存、队列上限、旧版隔离测试 |
| A23 | 两层模型开关及网络开关均执行；直接/分批模型调用按输入、输出上限和单价预估预算；取消批次记录原因，保留成功结果；畸形用量保守预占 | 关闭时零调用、预算先于请求、部分失败保留、空事件数组合法；成本仍依赖配置单价，不是供应商账单保证 |
| A24 | 模型修改状态后重算覆盖；最终输出统一计算摘要与评分输入 | 风险下调为数据不足后，覆盖数与结果状态一致 |
| A25 | 保留五个完整财年加最新披露期；百分数转比率，区分金额/天数/倍数；按期间长度推导非自然财年起点；A 股指标接口未给期次时由报告日识别季度、中报、三季报和年报 | 五年窗口、HK ROE、自然与非自然财年、A 股缺失期次字段测试。接口不足五个年报期时明确标注，不生成不存在的数据；万科真实数据已复验 |
| A26 | 增加兼容数据库迁移，保存 `period_start/audited/consolidated`；JSON 保存完整原始事实；RuleResult 使用实际行业包 | 旧库迁移不丢行，多次迁移安全；元数据与行业归属往返验证 |
| A27 | 每次请求有独立 ID，日志和健康计数幂等落库；读连接退出时关闭，事务异常回滚 | 相同请求对象重复保存不增加计数；两次真实请求各计一次；连接关闭、事务及五路并发测试。旧重复统计没有擅自重写 |
| A28 | 真正启用 StrictUndefined；补齐 AI 模型字段；仅为历史可选元数据提供明确默认值 | 缺少必填字段报错；18 份历史结构和 1 份新版正式报告均能渲染 |
| A29 | 报告时间来自固定版本数据；在线与下载均由同一 payload 和模板生成 | 同次版本在线、下载、生成文件字节一致；旧版在线与下载也一致 |
| A30 | 手机端将五列表格转换为带字段标签的卡片，并优化标题、目录、评分、详情和证据折叠区；锚点展开目标卡片内折叠区 | 320/390/430px 手机视口无整页横向溢出，1440px PC 保持五列表格；目标 `details.open=true`；未修改打印前后展开/恢复逻辑 |
| A31 | JSON 对象、query 字符串/长度、force 布尔值校验；不合法输入返回 400，队列满返回 429 | 数组/null/数字/字符串及非法字段类型均不再导致 500 |

## 测试用例迁移说明

原始诊断目录及失败日志保持原样。可持续运行的测试移至 `tests/test_acceptance.py`，并做了与修复契约一致的调整：

1. SSRF 测试改为明确期待阻断异常，同时继续断言没有访问私网。
2. 覆盖统计测试显式给待核验规则设置证据，避免依赖已删除的“任意年报兜底”。
3. 完成状态测试检查说明在 `notes`、`gaps` 为空，并补齐五个年报期的构造数据。
4. 地产公式用例显式披露合同负债为 0，不能用缺失代表 0。
5. 预算测试使用配置单价构造每批预估 0.3 元，不再依赖实现里写死的 0.3。
6. 原有评分渲染用例补上必填 `finding`，原有评分断言未放宽。

这些调整没有移除原来的错误行为断言；独立新增测试另行覆盖真实 `AStock` 格式、A 股缺失期次字段、币种、现金组成、完整符号组合、源码交付、安全连接、数据库迁移和在线下载一致性。

复跑：

```bash
/Users/shixiong/.workbuddy/binaries/python/envs/default/bin/python tests/run_tests.py
/Users/shixiong/.workbuddy/binaries/python/envs/default/bin/python -m unittest discover -s tests -p 'test_*.py' -v
/Users/shixiong/.workbuddy/binaries/python/envs/default/bin/python docs/fixes-2026-09-06/verify_delivery.py
```

## 文件与运行状态

- 测试日志：[原有 58 项](fixes-2026-09-06/baseline_results.log)、[53+36 项回归](fixes-2026-09-06/regression_results.log)。
- 浏览器证据：[新输入实际提交](fixes-2026-09-06/browser-new-security.txt)、[XSS 已阻断](fixes-2026-09-06/browser-xss.json)、[锚点展开](fixes-2026-09-06/browser-anchor.json)、[手机截图](fixes-2026-09-06/mobile-390.png)。
- 独立目录与历史兼容性：[delivery_results.json](fixes-2026-09-06/delivery_results.json)。
- 真实运行证据：[A/H 两市场扫描](fixes-2026-09-06/live_scan_v11.log)、[万科最终复验](fixes-2026-09-06/vanke_final_validation.log)、[正式 Web 扫描及在线/下载一致性](fixes-2026-09-06/web_scan.json)。`live_scan_v11.log` 中万科第一次失败是开发期间运行进程与随后更新模板的版本交叉；使用同一最终版本重跑已通过，失败记录保留用于追溯。
- 原始数据库在服务重启前使用 SQLite backup 生成一致性备份，位于 `fixes-2026-09-06/runtime/app-before-restart.db`。运行材料已排除 Git。
- 本地 Web 服务已用最终代码重启，地址仍为 `http://127.0.0.1:8770`。重启后健康接口正常、A 股代码查询正常，正式腾讯报告的在线页与下载文件逐字节一致（27,844 字节，规则版本 1.1）。本轮不修改 `.env`，不替换模型、不扩充规则数；运行时数据库、PDF、报告和密钥不纳入版本库。

## 验证边界

本轮修复了诊断清单对应的程序行为，不等于证明任意股票、任意财报、任意供应商响应均不会出错。原文金额行的存在性、来源币种交叉核对与完整会计语义确认有不同强度；没有足够依据时程序必须保留“部分支持/数据不足”。公开接口覆盖不足、原文下载/页数上限、模型供应商截断仍可能使扫描部分完成，现在均有实际原因可查。

没有扩展登录、对象存储、跨年事件追踪、分布式队列或 20 家人工标注集；没有把历史报告逐份改成新规则的结论。应重新扫描以获得规则 1.1 的结果。
