# A 股 / 港股基本面排雷 Agent V1.2 开发交接文档

> **最终产品决定（2026-09-06）**：本文是开发前任务书，其中“移除 A—E 公司等级”的原始要求
> 已由用户后续决定取代。最终实现保留醒目的 A—E 风险等级徽章（A 绿 / B 蓝 / C 黄 / D 橙 / E 红），
> 并保留风险信号评分免责说明。最终验收口径见 `docs/V1.2_ACCEPTANCE_RESULT.md`。

> 本文可直接作为 DeepSeek V4 Pro 的开发任务输入。请先完整阅读，再进入仓库执行。

## 一、你的角色和最终目标

你是本项目 V1.2 的主开发者。请直接在现有仓库完成代码、测试、真实复验和文档更新，不要只输出建议或伪代码。

V1.2 定位为 **可信度版本**。目标是在不增加规则数量、不降低现有报告质量的前提下，解决模型结果不稳定、AI 事件缺少证据、任务状态失真、历史事件缺少闭环、文档覆盖方式粗糙等问题。

仓库路径：`/Users/shixiong/Developer/pailei`  
开发基线：`main` 分支，提交 `be4477e`（优化报告模型批次并行耗时）  
规则版本：1.1  
现有规则：52 项  
现有自动测试：58 + 53 + 36，共 147 项  
当前报告运行时模型：GLM-5.3-Flash  
建议开发模型：DeepSeek V4 Pro，日常使用 high，复杂设计和最终审核使用 max

## 二、开始前必须阅读

按顺序阅读：

1. `README.md`
2. `HANDOVER.md`
3. `DEVELOPMENT_STATUS.md`
4. `docs/RULES.md`
5. `docs/FIX_REPORT_2026-09-06.md`
6. `docs/PERFORMANCE_OPTIMIZATION_2026-09-06.md`
7. `app/engine/pipeline.py`
8. `app/engine/runner.py`
9. `app/llm/adapter.py`
10. `app/data/pdftext.py`
11. `app/report/render.py`
12. `app/report/templates/report.html.j2`
13. `app/core/models.py` 与 `app/core/db.py`
14. `tests/run_tests.py`、`tests/test_acceptance.py`、`tests/test_hardening.py`

先检查工作区和当前提交；如工作区已有用户修改，不得覆盖或回退。创建独立开发分支，先运行两套现有测试并记录基线：

```bash
python tests/run_tests.py
python -m unittest discover -s tests -p 'test_*.py' -v
```

不要读取、打印或提交 `.env` 中的密钥。测试默认离线，不得依赖真实模型和公网才能通过。

## 三、当前能力与已验证基线

项目已经具备：

- A 股、港股证券识别和歧义确认；
- 东方财富财务数据、巨潮公告、港交所披露易公告；
- PDF 下载、解析、页码定位、片段指纹和生成后原文复核；
- 通用 36 项、银行/保险/券商/地产行业规则共 16 项；
- 模型分批解读、事件提取、独立核验、预算控制和最多 2 路并行；
- 自包含 HTML、在线报告、下载、历史记录、运行状态页面；
- PC 与 320/390/430px 手机端适配；
- 147 项自动测试全部通过。

真实样本：

| 样本 | 市场/行业 | 用途 |
|---|---|---|
| `600519.SH` 贵州茅台 | A 股/普通行业 | 盈利公司、审计意见误报保护 |
| `00700.HK` 腾讯控股 | 港股/普通行业 | 港股币种、现金口径、性能基准 |
| `000002.SZ` 万科 A | A 股/地产 | 亏损、负利润、地产行业规则、持续经营 |

腾讯正式 Web 报告在同条件优化后用时 61.383 秒，包含 1,234 条财务事实、168 条公告、25 份 PDF、50/50 条已复核证据和 52 项规则。这个数字是单次真实样本，不是固定 SLA。

## 四、必须保持的产品原则

以下原则优先级高于任何局部实现便利：

1. 模型不参与财务数值计算，不允许修改程序算出的指标。
2. 正式风险结论必须有可回溯来源；模型不能凭常识补充输入之外的事实。
3. 数据不足必须显式呈现，不能用默认值、估算值或隐藏检查项制造完整假象。
4. 不输出投资建议、爆雷概率或公司安全评级。
5. 严重程度、证据强度、当前是否仍存在必须分别表达。
6. 在线 HTML、下载 HTML 和 JSON 使用同一个结构化事实源。
7. 动态内容必须转义；外部 URL 只允许 HTTP/HTTPS；不得削弱现有 SSRF/XSS 防护。
8. 不允许通过减少公告、减少 PDF、截短输入、隐藏模型失败或放宽证据校验来提速。
9. 旧报告必须仍能查看；数据库迁移必须幂等、向后兼容且不伪造历史字段。
10. 不得为了让测试通过而修改测试期望、降低阈值或吞掉异常。只有契约确实改变时才同步更新旧测试，并在实施报告中解释。

## 五、V1.2 实施范围

按下列阶段依次实施。每阶段完成后运行全量测试并单独提交，避免一次性大改。

### 阶段 0：固定验收集和差异工具

先建立可重复验收框架，再修改业务逻辑。

要求：

- 建立不少于 20 家公司的验证清单，覆盖 A/H 两市场、普通行业、银行、保险、券商、地产，以及盈利、亏损、负现金流、非标审计、处罚、问询、诉讼、质押等情形。
- 设计人工标注格式：证券、数据快照、预期触发、禁止触发、证据位置、是否已解除、标注人、标注状态。
- 不得编造人工标签。尚未人工确认的公司必须标记 `pending_review`，不能算作通过样本。
- 把当前三个真实样本纳入清单，保留其规则 1.1 基准摘要。
- 增加报告语义对比工具，忽略任务 ID、生成时间、抓取时间等易变字段，比较证券身份、事实、指标、规则、证据、缺口和事件。
- 工具输出必须区分：确定性差异、模型差异、运行元数据差异。

验收：构造相同报告只改变时间字段时应判语义一致；改变财务值、规则状态、证据引文或缺口时必须检出。

### 阶段 1：模型结果缓存与可复现性

为模型调用增加持久化缓存，但不能绕过预算、校验或失败记录。

缓存键至少包含：

- 供应商接口标识、模型名；
- thinking/reasoning 配置、temperature、max tokens；
- 系统提示词和用户提示词的完整哈希；
- 提示词版本；
- 输入公告 ID、PDF SHA256 或等价内容快照；
- 业务步骤名称。

缓存值至少包含：原始响应或经过明确版本校验的结构化响应、用量、成本、生成时间、解析状态和校验版本。不得保存 API Key、Authorization 头或其他密钥。

要求：

- 同一输入和配置命中缓存后，结构化模型结果必须一致。
- 提示词、模型参数、文档内容或校验版本变化时必须失效。
- 截断、HTTP 失败、无法解析、结构非法的结果不得作为成功缓存复用。
- 并发请求同键时不得重复调用模型或写坏缓存。
- 报告和管理页区分“本次模型调用”和“缓存复用”，成本不能重复累计。
- 保留 `LLM_PARALLEL_CALLS=1` 串行回退能力。

### 阶段 2：AI 候选事件与正式事件分层

当前相同腾讯输入曾出现一次 3 条模型事件、一次 0 条；模型事件还可能没有 `evidence_ids`。V1.2 必须修正这一产品语义。

要求：

- 模型只能生成“候选事件”，不能直接成为正式风险事件。
- 模型输出必须带 `doc_id` 和 `evidence_quote`；日期始终取程序掌握的公告日期，不采信模型自由生成的日期。
- `doc_id` 必须属于本批输入；事件类型必须来自固定枚举；字符串长度、布尔字段和返回形状必须校验。
- 程序必须在相应 `ParsedDoc` 中定位完整引文，生成带文档、页码、指纹的 Evidence，并再次执行现有原文复核。
- 只有证据复核通过的候选事件才能升级为正式事件，并拥有至少一个 verified evidence ID。
- 未通过者进入“待核实线索”，说明失败原因，不参与正式风险计数、扣分和已确认时间线。
- 报告中将“已确认事件”和“待核实线索”明确分开；没有待核实线索时不显示空区块。
- 非法文档 ID、虚构引文、同前缀但尾部不同、错误页面、错误指纹必须拒收。

不得简单删除 AI 事件功能，也不得为了稳定而把模型输出全部隐藏。

### 阶段 3：事件生命周期与按需历史追溯

将同一事项的披露串成生命周期：

```text
首次发生 → 补充说明 → 回复/整改 → 最新进展 → 已解除或仍未解除
```

优先支持：监管问询与回复、立案与处罚、诉讼进展、股权质押/冻结与解除、盈利预警与正式财报、审计机构变更与后续聘任。

要求：

- 先执行现有 12 个月扫描；只有发现未解除的重要事件时，才针对事件类别按需向前追溯，最长 3—5 年。
- 设置单事件查询次数、公告数量、下载量和总耗时上限；达到上限必须记录缺口。
- 关联必须保留依据。公告编号、案件号、当事人、标题关键词、关联文件等确定性字段优先，模型只能辅助提出关联候选。
- “已解决”必须由后续正式披露支持，不能仅凭时间经过或模型判断。
- 同一事件的多份公告不能重复计数、重复扣分。
- 数据库和 JSON 保存事件关系、当前状态、状态依据及关联公告。

### 阶段 4：任务状态、覆盖等级和评分表达

当前运行库中的报告经常因为正常覆盖限制统一显示“部分完成”，状态已经难以区分真正失败。

要求：

- 将“任务是否成功生成”和“报告覆盖程度”拆开。
- 任务状态至少区分：排队、运行、生成成功、生成失败、超时。
- 报告覆盖至少区分：完整、一般缺口、关键缺口。
- 为 gap 建立结构化类型和严重度。缺少核心财务报表、公告清单失败、身份错误属于关键缺口；文件页数超过通用上限等可属于一般缺口；计算口径说明不是失败缺口。
- 状态页、历史页、API、JSON 和报告使用一致语义，并兼容旧任务状态。
- 保留报告顶部醒目的 A—E 风险等级徽章，按 A 绿 / B 蓝 / C 黄 / D 橙 / E 红展示，便于用户快速查看。
- 0—100 数字与 A—E 徽章都表示“风险信号密度”分档，不得表述为投资价值、违约概率或公司安全等级；同时展示最高已确认风险、风险/关注数量、有效覆盖率和证据复核率。
- 数据不足不能扣风险分，但必须单独影响覆盖可信度展示。

### 阶段 5：资料选择和目标章节解析

保留下载数量和任务期限保护，同时提高有限资源的有效覆盖。

要求：

- 文档排序从单一全局排名改为带类别配额的选择：财报/审计、处罚/问询、诉讼/冻结/担保、治理变化等重要类别不能被大量例行公告挤出前 25 份。
- 同类例行公告设置合理上限，选择原因写入运行诊断信息。
- 对长 PDF 优先利用目录、书签、页标题或轻量关键词定位审计意见、持续经营、诉讼、担保、受限资产、关联交易、债务等章节。
- 目标章节位于第 120 页之后时允许定向补充解析；不要默认把所有长 PDF 全文解析。
- 每个扩展解析动作受总页数、超时、文件大小和进程隔离限制。
- 报告区分“按上限截断但重点章节已覆盖”和“关键章节未定位”。
- 解析缓存必须按 PDF SHA256 和解析器版本失效，不能因文件名相同误复用。

### 阶段 6：行业规则能力口径

不新增行业规则。为每条规则增加能力状态：`enabled`、`unsupported_source`、`not_applicable` 等等价明确状态。

要求：

- 银行资本充足率、保险偿付能力、券商净资本等当前无可靠数据字段的检查，不计入“本次已执行的有效检查数量”。
- 仍可在“尚缺数据能力”中展示，但不能宣传成已完成检查。
- 覆盖率分母、行业摘要和 README 的规则说明同步修正。
- 如果现有公开披露文件能够稳定提取某个指标，可以实现并纳入；必须提供跨样本测试、单位口径和原文证据，不得凭模型估算。

### 阶段 7：运行诊断和存储控制

增加支持可信度改造所需的最小运维能力：

- 记录八个流水线阶段的开始、结束、墙钟耗时和结果摘要；
- 管理页展示各阶段耗时、模型缓存命中率、PDF 解析缓存命中率；
- 原始文件按内容指纹去重，避免同一 PDF 因重复扫描保存多份；
- 增加安全的缓存/旧报告清理命令，默认预览将删除内容，明确参数后才实际删除；
- 不自动删除用户报告或唯一原文。

## 六、V1.2 不做的功能

以下内容留到后续版本，本轮不要顺手实现：

- 自选股、批量扫描、定时监控和通知；
- 报告版本对比的产品页面；
- 用户登录、邀请制、权限和团队协作；
- PostgreSQL、Redis、Celery、S3 和分布式部署；
- 股价、估值、涨跌预测、新闻情绪、买卖建议；
- 聊天问答、PDF 导出和装饰性图表扩充；
- 更换当前报告运行时模型或数据供应商。

如某项是实现 V1.2 的必要前置，只实现最小基础能力，并在实施报告中说明，不得扩大产品范围。

## 七、测试要求

所有新测试必须离线、确定性、可重复。至少覆盖：

1. 模型缓存命中、失效、损坏、失败不缓存和同键并发；
2. 缓存命中不重复计费、不重复增加调用次数；
3. 相同数据快照连续运行三次，确定性结果和缓存后的模型结果语义一致；
4. AI 事件合法 doc ID、完整引文、正确页码和正确指纹；
5. 虚构引文、错误 doc ID、错误页码和非法返回结构被拒收；
6. 无 verified evidence 的事件不能进入正式时间线和风险计数；
7. 多份进展公告合并、解除依据、未解除状态和不重复扣分；
8. 历史追溯只在需要时启动，数量和时间上限有效；
9. 任务成功但有一般缺口、关键数据缺失、超时、彻底失败的状态组合；
10. 旧状态、旧报告 JSON 和数据库迁移向后兼容；
11. 新报告保留醒目的 A—E 风险等级徽章，颜色、风险数量、覆盖率和证据率正确；
12. 文档类别配额不会被例行公告挤占；目标章节可在 120 页之后定向解析；
13. PDF 缓存按内容和解析器版本失效；
14. 行业规则能力状态和覆盖率分母正确；
15. 阶段耗时、缓存命中率和清理预览准确；
16. XSS、SSRF、预算、任务期限、在线/下载一致性和手机端原回归继续通过。

不要编写只重复实现细节、没有失败可能的形式测试。每个测试应对应明确的产品风险或历史缺陷。

## 八、真实复验要求

完成离线测试后，使用独立任务 ID 强制重扫：

```bash
python scripts/run_scan.py 600519
python scripts/run_scan.py 00700
python scripts/run_scan.py 000002.SZ
```

真实复验必须记录：

- 证券身份、报告期、财务事实数量；
- 公告总量、下载/解析数量及选择原因；
- 证据数量、正式事件数量、待核实线索数量；
- 52 项规则中有效执行、数据不足、不适用、数据源暂不支持的数量；
- 风险/关注结论及与规则 1.1 基线的差异；
- 每个阶段耗时、总耗时、模型调用/缓存命中、成本和失败；
- 报告状态与覆盖等级；
- 在线和下载 HTML 是否一致，PC/手机是否无明显布局回归。

真实性边界：外部数据和模型输出会随时间变化，不能要求结论逐字相同。所有差异必须分类解释，不能只写“测试通过”。若出现新增风险、风险消失或证据变化，必须人工抽核原文后再判断是否回归。

性能要求：不得通过减少材料获得更快结果。腾讯无缓存模型调用的总耗时应与 61.383 秒基准处于合理波动范围；如果公网或模型延迟导致超出，记录各阶段耗时说明原因。模型和 PDF 缓存命中后的重复扫描应有可测量的耗时下降。

## 九、提交和文档要求

建议按以下边界提交：

1. `test: 建立 V1.2 验收集与语义差异工具`
2. `feat: 增加模型结果缓存与并发去重`
3. `feat: 强化 AI 事件证据绑定与候选分层`
4. `feat: 增加事件生命周期与按需历史追溯`
5. `refactor: 拆分任务状态覆盖等级与风险展示`
6. `feat: 优化披露文件选择与目标章节解析`
7. `refactor: 明确行业规则数据能力状态`
8. `feat: 增加阶段诊断缓存统计与存储清理`
9. `docs: 更新 V1.2 文档和验收结果`

完成时必须更新：

- `README.md`
- `HANDOVER.md`
- `DEVELOPMENT_STATUS.md`
- `docs/RULES.md`（规则数量不变，但能力状态和覆盖口径变化）
- `.env.example`（新增配置必须有安全默认值和中文说明）
- 新建 `docs/V1.2_IMPLEMENTATION_REPORT.md`

实施报告至少包含：最终行为、数据库迁移、接口变化、测试总数、真实样本对比、性能结果、剩余限制、提交列表和回滚方法。

## 十、完成定义

只有同时满足以下条件才能宣布 V1.2 完成：

- 原有 147 项测试全部通过；
- 所有新增 V1.2 测试通过；
- 三个真实样本全部生成可打开报告；
- 正式 AI 事件 100% 绑定至少一条通过复核的原文证据；
- 同一模型输入缓存后结果可复现，失败响应不污染缓存；
- 事件进展不重复计数，解除状态有正式披露依据；
- 任务成功、覆盖等级和 gap 严重度能正确区分；
- 新报告按最终产品决定保留醒目的 A—E 风险等级徽章；
- 无数据能力的行业检查不再冒充已执行规则；
- 在线、下载、JSON、数据库保持一致；
- 安全、预算、超时、并发、旧报告兼容和手机端无回归；
- 文档与实际实现一致，没有把未完成项写成已完成。

如果因数据授权、外部接口或缺少人工标注无法完成某项，先完成所有不依赖该阻塞的工作，在实施报告中写清：阻塞事实、已完成部分、可复现证据和继续所需条件。不得用模拟数据宣称真实能力已经完成。

## 十一、最终回复格式

最终向项目负责人回复时，按以下顺序：

1. 一句话说明 V1.2 是否达到完成定义；
2. 已实现功能及用户可见变化；
3. 未完成或有条件完成的内容；
4. 测试结果：原有、新增、总计；
5. 三个真实样本和性能结果；
6. 关键文件和提交列表；
7. 数据迁移、启动及回滚说明；
8. 需要负责人决策的问题。

不要只报告“代码已完成”或“测试通过”。必须给出可以复核的文件、任务 ID、报告路径、数量和差异说明。
