# 开发状态记录

用途：跨会话接续开发。每完成一个阶段更新一次，记录已完成、验证结果、剩余工作、阻塞项与下一步。

---

## 当前状态（更新于 2026-09-05 晚）

**一句话**：方案中的可执行内容已全部实现，A 股与港股端到端真实闭环跑通，本地 Web 应用在运行中。
AI 解读与核验已启用（GLM-5.3-Flash，实测单次扫描约 ¥0.05，6/6 异常解读成功）。

## 〇、2026-09-05 晚间迭代（LLM 接入 + 报告重构）

### LLM 接入与截断修复
- `.env` 配置 GLM-5.3-Flash（智谱 OpenAI 兼容协议），实测可用
- 发现并修复思考模型截断问题：GLM 推理内容在 `reasoning_content` 独立字段但计入
  completion_tokens，`max_tokens=4000` 时 JSON 被截断 → `chat_json` 识别
  `finish_reason=length` 返回明确错误；`interpret_anomalies` / `extract_events`
  改分批调用（每批 3 项异常 / 10 份文档），单批失败只丢该批
- 修复后 6/6 解读生效，单次扫描成本约 ¥0.045

### 报告重构（四轮迭代，9 节 → 4 节，体积 -65%）
- 章节结构：**风险总览 → 逐项检查结果 → 问题详情与原文 → 数据来源与算法**；
  附报告目录（锚点跳转 + 打印自动展开 + 锚点联动展开折叠区）
- 01/03 去重：01 改"发现清单"索引表（点击跳 03 证据卡）
- 02 矩阵过滤：`rejectattr` 渲染层过滤"不适用"与"数据不足"行（JSON 数据层不动）
- 02 说明列：why > 60 字折叠为"展开说明（N 字）"
- 标题格式：`证券简称（代码）排雷报告`

### 风险信号评分（新增）
- `risk_signal_score()`：基础 100，发现风险 高-12/中-8/低-4，需要关注 中-2/低-1，
  下限 0；评级 A≥95/B≥80/C≥60/D≥35/E<35；渲染层计算，历史 JSON 无需迁移
- 已知样本：茅台 97A / 腾讯 100A / 兆易创新 95A / 赛轮 90B / 万科 24E

### 身份识别修复
- `_resolve_exact`（显式代码直达路径）构造的 Security `name=code`，enrich 只补 org_name
  → enrich 增加短名回填（search 接口按代码精确匹配取 Name），A/H 通吃
- 存量 603986 的 JSON 与 SQLite reports 表已同步补正（仅 security.name，不动分析数据）

### 视觉与交互
- 大栏目 h2：淡蓝色带 + 蓝色左标条；小栏目 h3：浅灰带 + 细蓝左标条（内嵌式，无负 margin）
- 移动端 640px 断点同步适配

### 测试
- 47 → **58 项**：新增 TestRiskScore 11 项（评分数学、评级边界、未知严重度、构成串）+
  渲染断言（评分块、无"风险等级分布"残留、标题格式）

## 一、已完成

### 阶段 0：数据验证 ✅（2026-09-05）
- `scripts/phase0_validate.py` 对 600519（A）与 00700（HK）做真实接口验证
- A 股链路（身份 + 财报 + 公告 + 原文）：**通过**；港股链路：**通过**
- 报告：`docs/PHASE0_DATA_VALIDATION.md`（自动生成，全部数字来自真实调用）
- 实测要点：
  - 东方财富数据中心无需鉴权，A 股三表 + 主要指标、港股三表（长表 STD_ITEM_CODE 结构）均可取
  - 巨潮 orgId 构造规则不统一（gssh0600519 / gssz0000001 / GD165627），必须走 topSearch 解析
  - 披露易 titleSearchServlet 的 page/startRow 参数无效，分页靠时间窗口二分实现（已实测 172/172 全量取回）
  - 港股披露为繁体中文，证据定位需要简繁双字形（`app/core/text.py`）

### 阶段 1：产品骨架 ✅
- FastAPI 应用（`app/main.py`）：首页搜索（候选项 + 歧义确认）、扫描进度页（轮询）、
  报告页、历史记录、运行状态页（数据源健康 / 模型消耗 / 任务统计）
- 后台任务：进程内线程池（上限 5 并行，对应方案内测规模目标）
- SQLite 持久化（`app/core/db.py`）：任务、财务事实、文档、证据、规则结果、
  风险事件、报告版本、抓取日志、数据源健康、模型用量，共 10 张表
- 同一查询 60 分钟内去重复用；显式证券代码（000002.SZ / 00700.HK）直达不进歧义流程

### 阶段 2：资料与证据 ✅
- 数据适配层：`eastmoney.py`（A+HK 财报）、`cninfo.py`（A 股公告+PDF）、
  `hkexnews.py`（港股公告+PDF）、`identity.py`（识别 + A/H 启发式关联，标注待核实）
- 安全 HTTP 客户端：限流（每主机 2 rps）、重试退避、SSRF 防护（禁内网）、体积上限、全程留痕
- PDF 解析（pdfplumber）+ 证据定位：片段绑定页码、内容指纹、事后原文复核
- 财务标准化：原始科目 + 标准科目、币种、报告期类型、累计口径、审计标记、来源 URL 全部落库

### 阶段 3：风险引擎 ✅
- 通用规则 36 项（`app/engine/rules/general.py`），行业包 16 项（`industry.py`），共 52 项配置化规则
- 指标全部由程序计算（`metrics.py`），边界处理：
  - 零分母 / 缺失 → None，不产出 inf/nan
  - 负利润不套用普通同比（返回 None 并在报告中说明）
  - 同比严格同口径（中报对中报）
  - 受限资金从可用现金中扣除
  - 重要性过滤：应收/存货占总资产 < 2% 时增速类指标不形成结论
- LLM 适配层（`app/llm/adapter.py`）：OpenAI 兼容协议可替换；未配置密钥时整体跳过并标注，绝不模拟
- 独立核验：证据原文复核（程序）+ 结论-证据一致性复核（模型，可选）

### 阶段 4：报告交付 ✅
- 受控 Jinja2 模板（StrictUndefined，未定义变量直接报错而非渲染空白）
- 独立 HTML：内联 CSS + 内联 SVG，实测 0 外部脚本/字体/图片，离线可读
- 动态文字转义、链接仅 http/https（有单元测试覆盖）
- 在线报告与下载内容完全一致（同一模板渲染）

### 测试与验证 ✅
- `tests/run_tests.py`：58 项确定性测试全部通过，覆盖方案 §11 边界样本
  （负利润、零分母、累计 vs 单季、简繁字形、审计意见误报排除、证据真实性、XSS 转义、URL 协议白名单、评分数学与评级边界）
- 端到端真实样本 5 例（见 README 表格），含行业包路由验证与 AI 解读启用样本
- Web 完整流程验证：提交 → 轮询进度 → 报告页 → 下载，全部 200

## 二、开发中发现并修复的问题（有测试回归保护）

| 问题 | 根因 | 修复 |
|---|---|---|
| 利息保障倍数对茅台报 -252 倍高风险 | 财务费用为负（利息净收入）时该倍数无经济含义 | `SV04` 先判财务费用符号，为负则判"不适用"并说明 |
| 茅台被报"保留意见" | 半年报固定勾选项「非标准审计意见涉及事项 □适用 √不适用」命中关键词 | `scan_audit_opinions` 改为断言式匹配 + 模板排除 + 否定词识别 + 命中区间去重 |
| 茅台被报"审计机构变更" | 续聘/履职评估类公告被当成变更 | `GV01` 区分变更（改聘/更换）与例行（续聘/履职评估）|
| 腾讯被报"存货增速风险" | 存货仅占总资产 0.032%，小基数增速无意义 | FQ03/FQ05 增加重要性门槛（< 2% 总资产不形成结论）|
| 腾讯被报"高管变动" | 股东会例行文件（重选董事、代表委任表格）被误分类 | `_classify_hk` 先识别例行文件再匹配风险类型 |
| 行业包启用后通用规则全部跳过 | packs 设计为白名单 | 增加 `exclude_packs` 排除制：特殊行业保留财务质量/治理/监管类通用规则 |

## 三、验证结果汇总（2026-09-05 实测）

| 指标 | 实测值 | 方案目标 | 判定 |
|---|---|---|---|
| 股票搜索响应 | < 1 秒 | 2 秒内 | 达标 |
| 首次完整扫描（A 股） | 24—37 秒 | 3—10 分钟 | 显著优于目标 |
| 首次完整扫描（港股） | 55—70 秒 | 3—10 分钟 | 显著优于目标 |
| 单元测试 | 47/47 通过 | 预设计算样例全部通过 | 达标 |
| 证据真实性 | 63/63、44/44、89/89 复核通过 | 不允许虚构来源 | 达标 |
| 公告完整取回 | A 股 94/94、港股 172/172（声明数） | 记录分页与失败 | 达标 |
| 并行任务 | 线程池上限 5 | 5 个并行扫描 | 达标（未做满载压测）|

## 四、未实现 / 未启用 / 未核验

按方案要求分别标注，不以完成口径表述：

**未启用（配置问题，代码已就绪）**
- [x] AI 解读与独立核验：**已于 2026-09-05 启用**（GLM-5.3-Flash，`.env` 配置），
      实测 6/6 异常解读成功、单次约 ¥0.05；换服务商只需改 `.env` 三个变量
- [x] 模型单价与成本折算：`LLM_PRICE_*` 已配置（智谱定价 1.1 / 3.6 元每百万 token）

**未实现（方案范围内，后续版本）**
- [ ] 用户登录与邀请制内测（方案 §2：第一阶段采用邀请制；当前为本地单用户形态）
- [ ] 历史风险向前追溯与后续进展自动检索（方案 §1"历史风险"：当前仅按 12 个月公告窗口检查，
      时间线已具备，跨年追溯未做）
- [ ] S3 兼容对象存储（当前原文与报告存本地 `data/` 目录）
- [ ] Docker Compose 部署编排（本地可运行版本已达成，方案 §7 的部署层未做）
- [ ] 20 家固定验证集回归（方案 §11：当前 4 例人工验证样本，未形成可重复的回归集）
- [ ] Celery + Redis 任务队列与 PostgreSQL（本地版以线程池 + SQLite 等价实现，
      重启后可从历史任务恢复查看，但运行中任务的断点续跑未做）

**未核验（数据权限，见 §五）**
- [ ] 商用授权：三个数据源均未确认商业使用、缓存与再分发条款

## 五、阻塞项与风险

| 项 | 影响 | 应对 |
|---|---|---|
| 数据源为公开网页接口，无 SLA | 字段变更会导致扫描失败率上升 | 适配层已隔离（`app/data/`），健康监控在运行状态页；生产需评估授权数据服务 |
| 无 LLM 密钥 | AI 解读/核验未启用 | 已解决：GLM-5.3-Flash 已配置并实测通过；思考模型截断问题已修复（分批调用） |
| 巨潮对高频抓取敏感 | 公开部署可能被限流 | 每主机 2 rps 限流已生效；公告结果可按公司+快照复用 |

## 六、下一步建议（按优先级）

1. ~~配置 `LLM_API_KEY`，跑一次带 AI 解读的完整扫描~~ ✅ 已完成（GLM-5.3-Flash，兆易创新样本验证）
2. 建立 20 家固定验证集（`scripts/run_scan.py` 批量跑 + 人工标注预期结论），沉淀为回归脚本
3. 实现历史风险跨年追溯（对时间线中未结事项扩展公告检索窗口）
4. 与数据方确认商用条款；如需公开部署，先加登录与权限绑定，再补 Docker Compose
5. 迁移 PostgreSQL + 独立任务队列（若并发规模超出单机线程池）

## 七、会话续接指引

- 启动：`python -m uvicorn app.main:app --host 127.0.0.1 --port 8770`
- 当前运行实例：http://127.0.0.1:8770 （本机）
- 规则在哪改：`app/engine/rules/general.py`（通用）、`app/engine/rules/industry.py`（行业包）；规则清单与阈值参考 `docs/RULES.md`，改规则需同步更新该文档
- 指标在哪改：`app/engine/metrics.py`
- 新数据源怎么接：参照 `app/data/eastmoney.py` 的适配器模式，实现后替换 pipeline 中的调用
- 数据库表结构：`app/core/db.py` 顶部 SCHEMA
- 报告模板：`app/report/templates/report.html.j2`（在线与下载共用，改一处两边生效）
